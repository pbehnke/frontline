"use strict";

var cloneHeaders = require('./CloneHeaders');

var ResponseManipulator = function() {};

ResponseManipulator.prototype.buildResponse = function(originalResponse, modifiedResponse) {
};

module.exports = ResponseManipulator;
